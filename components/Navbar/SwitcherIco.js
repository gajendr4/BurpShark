import { Switcher } from "@carbon/react/icons";

export default function SwitcherIco() {
  return (
    <>
      <Switcher size={20} />
    </>
  );
}

// Because the icon had similar import so we ought to have this for not running into problem
